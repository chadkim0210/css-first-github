처음일경우
1. 새로운 폴더 생성
2. 새로운 폴더에 들어가 git init
3. git remote add (origin: nickname) (repository address) : 받는 사람 등록

<checkpoint>
4. 새로운 파일 생성 + 작업
5. git add 새로운 파일이름으로 파일을 추가
6. git commit -m "message" 
7. git push (origin: registered nickname from #3) master 

<checkpoint>
